[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Optional but recommended: keep the order endpoint out of the CDN cache.
[[headers]]
  for = "/.netlify/functions/order"
  [headers.values]
    Cache-Control = "no-store"
